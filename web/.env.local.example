# Python FastAPI backend (Railway deployment)
# Paste your Railway URL — no trailing slash
BOT_API_URL=https://your-railway-app.up.railway.app

# Must match DASHBOARD_SECRET in bot/.env
# Default value shown — change in production
BOT_API_SECRET=ops-tasks-secret-change-me
